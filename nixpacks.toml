[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm cache clean --force", "npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["rm -rf dist", "npm run build"]

[start]
cmd = "node dist/index.cjs"
